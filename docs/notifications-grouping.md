# Notification grouping tabs

Account notifications page should group items by type with filter tabs
(Transactions, Security, Marketing). Persist the selected tab in the URL query
string so refreshes keep context.

Unread counts appear on each tab badge; mark-all-read clears only the active group.
