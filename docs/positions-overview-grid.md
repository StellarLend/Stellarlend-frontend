# Positions overview grid

Dashboard should render a sortable grid fed by `usePositions`:

- Default sort: ascending health factor (riskiest first)
- Columns: asset, supplied, borrowed, health, liquidation price
- Link each row to the asset detail route

Empty state copy when the wallet has no open positions.
