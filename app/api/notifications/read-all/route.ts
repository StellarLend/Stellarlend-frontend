import { NextResponse } from 'next/server'; import { getUser } from '@/lib/auth'; import { markAllNotificationsRead } from '@/lib/notifications/repository'; import { withCsrfProtection } from '@/lib/api/handler'; export const runtime = 'nodejs'; const patchHandler = async () => { const user = await getUser(); if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); try { const updatedCount = await markAllNotificationsRead(user.id); return NextResponse.json({ updatedCount }, { headers: { 'Cache-Control': 'no-store', Pragma: 'no-cache' } }); } catch { return NextResponse.json({ error: 'Internal Server Error' }, { status: 500 }); } }; export const PATCH = withCsrfProtection(patchHandler);